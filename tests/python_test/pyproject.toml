[project]
name = "python-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "accelerate>=1.13.0",
    "numpy>=2.2.6",
    "torch>=2.11.0",
    "transformers>=5.5.4",
]
